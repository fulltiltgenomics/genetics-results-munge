data
*.tsv
*.tsv.gz
*.tsv.gz.tbi
__pycache__/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Environment
# every .env variant holds real credentials; the committed example does not
.env*
!.env.example
